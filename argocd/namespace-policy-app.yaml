apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: namespace-policy-app
  namespace: openshift-gitops
  labels:
    app: global-hub-policies
spec:
  project: default
  source:
    repoURL: https://github.com/bjoydeep/gh-policy.git  # Update this with your Git repo URL
    targetRevision: main
    path: policies
  destination:
    server: https://cluster-proxy-addon-user.multicluster-engine.svc.cluster.local:9092/mh-01  # mh-01 via ACM cluster-proxy
    namespace: gh-policy
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m